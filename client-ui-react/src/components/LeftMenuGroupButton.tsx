import React from 'react';
import { Button } from 'react-bootstrap';

class LeftMenuGroupButton extends React.Component {
  
  render() {
    return (
      <Button >All</Button>
    );
  }
}

export default LeftMenuGroupButton;  